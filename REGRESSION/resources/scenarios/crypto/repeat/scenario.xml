<scenario xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xmlns="http://www.testlum.com/testing/model/scenario"
          xsi:schemaLocation="http://www.testlum.com/testing/model/scenario scenario.xsd">

    <overview>
        <description>
            Verify that Crypto encrypt and decrypt work correctly when executed repeatedly.
        </description>
        <name>Crypto repeat test</name>
    </overview>

    <settings>
        <tags>crypto</tags>
    </settings>

    <var name="plainText" comment="Initial crypto value">
        <constant value="dota DOTA2"/>
    </var>

    <repeat comment="Check crypto command in repeat" times="3">

        <crypto comment="encrypt some vzlue"
                action="encrypt"
                alias="CRYPTO2"
                name="encryptedValue">
            <value>{{plainText}}</value>
        </crypto>

        <crypto comment="decrypt value in repeat"
                action="decrypt"
                alias="CRYPTO2"
                name="decryptedValue">
            <value>{{encryptedValue}}</value>
        </crypto>

    </repeat>

</scenario>
