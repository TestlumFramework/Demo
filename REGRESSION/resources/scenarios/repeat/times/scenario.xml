<scenario xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xmlns="http://www.knubisoft.com/testlum/testing/model/scenario"
          xsi:schemaLocation="http://www.knubisoft.com/testlum/testing/model/scenario scenario.xsd">

    <overview>
        <description>Self test for command: Repeat with times — verifies that commands inside repeat are executed on each iteration</description>
        <name>Repeat with times</name>
    </overview>

    <settings>
        <tags>repeat</tags>
    </settings>

    <var comment="Variable for result" name="result">
        <constant value="0"/>
    </var>

    <repeat comment="Increase result by 1 - two times" times="2">
        <var comment="Increase 'result' variable" name="result">
            <expression value="{{result}} + 1"/>
        </var>
    </repeat>

    <assert comment="Check result increased by 2">
        <equal comment="Check result increased by 2">
            <content>{{result}}</content>
            <content>2</content>
        </equal>
    </assert>

    <repeat comment="Execute repeat inside repeat two times" times="2">
        <repeat comment="Increase result by 1 - two times" times="2">
            <var comment="Increase 'result' variable" name="result">
                <expression value="{{result}} + 1"/>
            </var>
        </repeat>
    </repeat>

    <assert comment="Check result increased by 4">
        <equal comment="Check result increased by 4">
            <content>{{result}}</content>
            <content>6</content>
        </equal>
    </assert>

</scenario>