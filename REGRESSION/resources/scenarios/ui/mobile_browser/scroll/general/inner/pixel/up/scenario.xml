<scenario xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xmlns="http://www.knubisoft.com/testlum/testing/model/scenario"
          xsi:schemaLocation="http://www.knubisoft.com/testlum/testing/model/scenario scenario.xsd">

    <overview>
        <name>Scroll - Inner Pixel Up</name>
        <description>
            This scenario verifies inner scroll up using pixel measure in mobile browser.

            Test flow:
            1. Navigate to Scroll Test page.
            2. Scroll down inner container.
            3. Scroll up inner container.
            4. Verify that top element becomes present.

            This scenario validates:
            - inner scroll up
            - pixel measure
            - reverse scroll behavior
        </description>
    </overview>

    <settings>
        <tags>mobilebrowser</tags>
    </settings>

    <include comment="Include login step"
             scenario="/ui/includes/login_mobile_browser/scenario.xml"/>

    <mobilebrowser comment="Start MOBILE BROWSER script">

        <navigate comment="Navigate to Scroll Test page"
                  command="to"
                  path="/scroll-test"/>

        <wait comment="Wait for page load"
              time="2"/>

        <scroll comment="Scroll down inner container"
                type="inner"
                direction="down"
                value="300"
                measure="pixel"
                locator="scrollTest.inner"/>

        <wait comment="Wait for scroll down to stabilize"
              time="2"/>

        <scroll comment="Scroll up inner container"
                type="inner"
                direction="up"
                value="300"
                measure="pixel"
                locator="scrollTest.inner"/>

        <wait comment="Wait for scroll up to stabilize"
              time="2"/>

        <assert comment="Verify top element is present after scroll up">
            <present comment="Check inner top element"
                     locator="scrollTest.innerTop"/>
        </assert>

    </mobilebrowser>

</scenario>