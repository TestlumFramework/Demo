<scenario xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xmlns="http://www.knubisoft.com/testlum/testing/model/scenario"
          xsi:schemaLocation="http://www.knubisoft.com/testlum/testing/model/scenario scenario.xsd">

    <overview>
        <name>Assert Present - Variable</name>
        <description>
            This scenario verifies present assertion using variables in mobile browser.

            Test flow:
            1. Store locator in variable.
            2. Execute present assertion using variable.

            This scenario validates:
            - Variable injection
            - Dynamic locator usage
        </description>
    </overview>

    <settings>
        <tags>mobilebrowser</tags>
    </settings>

    <include comment="Include login steps" scenario="/ui/includes/login_mobile_browser/scenario.xml"/>

    <mobilebrowser comment="Start MOBILE BROWSER script">

        <var comment="Locator variable" name="locator">
            <constant value="forms.save"/>
        </var>

        <var comment="Assert comment variable" name="assertComment">
            <constant value="Check presence via variable"/>
        </var>

        <var comment="Present Comment variable" name="presentComment">
            <constant value="Check element presence"/>
        </var>

        <var comment="Navigate comment" name="navigateComment">
            <constant value="Navigate to 'tables page' for apply the 'assert' command"/>
        </var>

        <var comment="Path variable" name="path">
            <constant value="/forms"/>
        </var>

        <navigate comment="{{navigateComment}}"
                  command="to"
                  path="{{path}}"/>

        <assert comment="{{assertComment}}">
            <present comment="{{presentComment}}"
                     locator="{{locator}}"/>
        </assert>

    </mobilebrowser>

</scenario>