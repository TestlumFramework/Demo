<scenario xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xmlns="http://www.knubisoft.com/testlum/testing/model/scenario"
          xsi:schemaLocation="http://www.knubisoft.com/testlum/testing/model/scenario scenario.xsd">

    <overview>
        <name>Clear - General</name>
        <description>
            This scenario verifies clear command functionality in mobile browser.

            Test flow:
            1. Navigate to forms page.
            2. Input value into field.
            3. Store value before clear.
            4. Execute clear command.
            5. Verify value is no longer equal to original.

            This scenario validates:
            - Clear command execution
            - Field value reset behavior
        </description>
    </overview>

    <settings>
        <tags>mobilebrowser</tags>
    </settings>

    <include comment="Include login steps"
             scenario="/ui/includes/login_mobile_browser/scenario.xml"/>

    <mobilebrowser comment="Start MOBILE BROWSER script">

        <navigate comment="Navigate to UI Elements page"
                  command="to"
                  path="/ui-elements"/>

        <scrollTo comment="Scroll to Clear section" locator="uiElements.clearInput"/>

        <input comment="Enter value into input"
               locator="uiElements.clearInput"
               value="TestUser"/>

        <assert comment="Verify value is displayed">
            <attribute comment="Check displayed value"
                       name="innerText"
                       locator="uiElements.clearResult">
                <content>Value: TestUser</content>
            </attribute>
        </assert>

        <clear comment="Clear input field"
               locator="uiElements.clearInput"
               highlight="true"/>

        <wait comment="Wait 1 second" time="1"/>

        <assert comment="Verify value is empty after clear">
            <attribute comment="Check empty state"
                       name="innerText"
                       locator="uiElements.clearResult">
                <content>Value: empty</content>
            </attribute>
        </assert>

    </mobilebrowser>

</scenario>