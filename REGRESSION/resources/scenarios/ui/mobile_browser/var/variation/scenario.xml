<scenario xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xmlns="http://www.knubisoft.com/testlum/testing/model/scenario"
          xsi:schemaLocation="http://www.knubisoft.com/testlum/testing/model/scenario scenario.xsd">

    <overview>
        <name>Var - Variation</name>
        <description>
            This scenario verifies var usage with CSV variations and full injection in mobile browser.
        </description>
    </overview>

    <settings>
        <variations>varVariation.csv</variations>
        <tags>mobilebrowser</tags>
    </settings>

    <include comment="Include login step"
             scenario="/ui/includes/login_mobile_browser/scenario.xml"/>

    <mobilebrowser comment="{{webComment}}">

        <navigate comment="{{navigateComment}}"
                  command="to"
                  path="{{path}}"/>

        <wait comment="{{waitComment}}"
              time="{{waitTime}}"/>

        <var comment="{{varComment}}"
             name="{{varName}}">
            <constant value="{{value}}"/>
        </var>

        <input comment="{{inputComment}}"
               locator="{{locator}}"
               value="{{value}}"/>

        <assert comment="{{assertComment}}">
            <attribute comment="{{attributeComment}}"
                       name="{{attributeName}}"
                       locator="{{locator}}">
                <content>{{value}}</content>
            </attribute>
        </assert>

    </mobilebrowser>

</scenario>