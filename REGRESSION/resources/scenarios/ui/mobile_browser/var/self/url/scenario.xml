<scenario xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xmlns="http://www.knubisoft.com/testlum/testing/model/scenario"
          xsi:schemaLocation="http://www.knubisoft.com/testlum/testing/model/scenario scenario.xsd">

    <overview>
        <name>Var - URL (UI usage)</name>
        <description>
            This scenario verifies var url usage in mobile browser.

            Test flow:
            1. Navigate to page.
            2. Capture current URL.
            3. Input URL into field.
            4. Verify that value is applied.
        </description>
    </overview>

    <settings>
        <tags>mobilebrowser</tags>
    </settings>

    <include comment="Include login step"
             scenario="/ui/includes/login_mobile_browser/scenario.xml"/>

    <mobilebrowser comment="Start MOBILE BROWSER script">

        <navigate comment="Navigate to UI Elements page"
                  command="to"
                  path="/ui-elements"/>

        <wait comment="Wait for page load"
              time="2"/>

        <var comment="Get current URL"
             name="currentUrl">
            <url/>
        </var>

        <input comment="Input URL into field"
               locator="uiElements.hotKeyInput1"
               value="{{currentUrl}}"
               highlight="true"/>

        <assert comment="Verify input contains URL">
            <attribute comment="Verify input contains URL"
                       name="value"
                       locator="uiElements.hotKeyInput1">
                <content>{{currentUrl}}</content>
            </attribute>
        </assert>

    </mobilebrowser>

</scenario>