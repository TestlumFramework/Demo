<scenario xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xmlns="http://www.knubisoft.com/testlum/testing/model/scenario"
          xsi:schemaLocation="http://www.knubisoft.com/testlum/testing/model/scenario scenario.xsd">

    <overview>
        <name>Javascript - Variations</name>
        <description>
            This scenario verifies javascript command using multiple JS files in mobile browser.

            Test flow:
            1. Navigate to UI Elements page.
            2. Execute javascript from different files.
            3. Verify result after each execution.

            This scenario validates:
            - CSV-based JS file injection
            - multiple JS execution
            - dynamic expected values
        </description>
    </overview>

    <settings>
        <variations>javascript.csv</variations>
        <tags>mobilebrowser</tags>
    </settings>

    <include comment="Include login steps"
             scenario="/ui/includes/login_mobile_browser/scenario.xml"/>

    <mobilebrowser comment="{{webComment}}">

        <navigate comment="{{navigateComment}}"
                  command="to"
                  path="{{path}}"/>

        <wait comment="{{waitComment}}"
              time="{{waitTime}}"/>

        <clear comment="Reset state before JS"
               locator="{{locator}}"/>

        <javascript comment="{{jsComment}}"
                    file="set_value.js"/>

        <wait comment="Wait after JS execution"
              time="1"/>

        <assert comment="{{assertComment}}">
            <attribute comment="{{attributeComment}}"
                       name="value"
                       locator="{{locator}}">
                <content>{{expected}}</content>
            </attribute>
        </assert>

    </mobilebrowser>

</scenario>