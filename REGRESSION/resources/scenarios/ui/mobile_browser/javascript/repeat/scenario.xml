<scenario xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xmlns="http://www.knubisoft.com/testlum/testing/model/scenario"
          xsi:schemaLocation="http://www.knubisoft.com/testlum/testing/model/scenario scenario.xsd">

    <overview>
        <name>Javascript - Repeat Increment</name>
        <description>
            This scenario verifies repeated execution of javascript command using increment logic in mobile browser.

            Test flow:
            1. Navigate to UI Elements page.
            2. Clear input.
            3. Execute javascript multiple times.
            4. Verify incremented value.

            This scenario validates:
            - repeat execution
            - javascript state persistence
            - correct increment behavior
        </description>
    </overview>

    <settings>
        <tags>mobilebrowser</tags>
    </settings>

    <include comment="Include login step"
             scenario="/ui/includes/login_mobile_browser/scenario.xml"/>

    <mobilebrowser comment="Start MOBILE BROWSER script">

        <navigate comment="Navigate to UI Elements page"
                  command="to"
                  path="/ui-elements"/>

        <wait comment="Wait for page load" time="2"/>

        <clear comment="Reset input before repeat"
               locator="uiElements.hotKeyInput1"/>

        <repeat comment="Execute JS increment 3 times"
                times="3">

            <javascript comment="Increment value via JS"
                        file="increment.js"/>

        </repeat>

        <assert comment="Verify final value is 3">
            <attribute comment="Check increment result"
                       name="value"
                       locator="uiElements.hotKeyInput1">
                <content>3</content>
            </attribute>
        </assert>

    </mobilebrowser>

</scenario>