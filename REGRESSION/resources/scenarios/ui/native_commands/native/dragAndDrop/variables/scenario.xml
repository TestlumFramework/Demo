<scenario xmlns="http://www.knubisoft.com/testlum/testing/model/scenario"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://www.knubisoft.com/testlum/testing/model/scenario scenario.xsd">

    <overview>
        <description>Testing dragAndDrop tag with variables</description>
        <name>Drag and drop tag</name>
    </overview>

    <settings>
        <tags>native</tags>
    </settings>

    <native comment="Executing native automation sequence">

        <click comment="Clicking on button dragAndDrop page" locator="testApp.dragAndDropPage"/>

        <var comment="Variable locator from"
             name="locFrom">
            <constant value="testApp.elementToBeFlipped"/>
        </var>
        
        <var comment="Variable locator to"
             name="locTo">
            <constant value="testApp.placeWhereElementWillBe"/>
        </var>

        <dragAndDrop comment="Run dragAndDrop"
                     fromLocator="{{locFrom}}"
                     fromLocatorStrategy="locatorId"
                     toLocator="{{locTo}}"
                     toLocatorStrategy="locatorId"/>

        <assert comment="Validating expected UI state">
            <attribute comment="Retrieving element text attribute for validation"
                       locator="//android.widget.TextView[@text='Dropped: 1']"
                       locatorStrategy="xpath"
                       name="text">
                <content>Dropped: 1</content>
            </attribute>
        </assert>
    </native>

</scenario>