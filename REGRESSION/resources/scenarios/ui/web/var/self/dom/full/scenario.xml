<scenario xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xmlns="http://www.knubisoft.com/testlum/testing/model/scenario"
          xsi:schemaLocation="http://www.knubisoft.com/testlum/testing/model/scenario scenario.xsd">

    <overview>
        <name>Var - DOM (Full) - Correct</name>
        <description>
            This scenario verifies full DOM extraction and parsing.

            Test flow:
            1. Navigate to UI Elements page.
            2. Capture full DOM.
            3. Extract element from DOM using XPath.
            4. Verify extracted value.

            This scenario validates:
            - full DOM extraction
            - variable storage
            - DOM parsing via path
        </description>
    </overview>

    <settings>
        <tags>web</tags>
    </settings>

    <include comment="Include login step"
             scenario="/ui/includes/login_web/scenario.xml"/>

    <web comment="Start WEB script">

        <navigate comment="Navigate to UI Elements page"
                  command="to"
                  path="/ui-elements"/>

        <wait comment="Wait for page load"
              time="2"/>

        <var comment="Get full DOM"
             name="fullDom">
            <dom/>
        </var>

        <var comment="Extract page title from DOM"
             name="pageTitle">
            <path fromVar="fullDom"
                  value="//h3/text()"/>
        </var>

        <assert comment="Verify page title is correct">
            <equal comment="Verify page title is correct">
                <content>{{pageTitle}}</content>
                <content>UI Elements</content>
            </equal>
        </assert>

    </web>

</scenario>