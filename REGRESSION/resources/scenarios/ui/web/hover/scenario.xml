<scenario xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xmlns="http://www.knubisoft.com/testlum/testing/model/scenario"
          xsi:schemaLocation="http://www.knubisoft.com/testlum/testing/model/scenario scenario.xsd">

    <overview>
        <description>Self test for command: hover</description>
        <name>Hover test</name>
    </overview>

    <settings>
        <tags>web</tags>
    </settings>

    <include comment="Include login steps" scenario="/ui/includes/login_web/scenario.xml"/>

    <web comment="Start WEB scripts">

        <wait comment="Wait for loading new page 1 seconds" time="1"/>

        <hover comment="Hover over the menu icon" locator=".//img[@alt='Your avatar']" locatorStrategy="xpath"/>

        <hover comment="Move the mouse over the logout button" locator="dashboard.logout"/>

        <wait comment="Wait 1 second" time="1"/>

        <click comment="Click on the 'Log out' button" locator="dashboard.logout"/>

        <wait comment="Wait for loading new page 1 seconds" time="1"/>

        <assert comment="Create assert for command's test">
            <attribute comment="Assert for hover" name="type" locator="login.signIn">
                <content>submit</content>
            </attribute>
        </assert>

        <wait comment="Wait for timeout scenario" time="1"/>
    </web>

</scenario>
