<scenario xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xmlns="http://www.knubisoft.com/testlum/testing/model/scenario"
          xsi:schemaLocation="http://www.knubisoft.com/testlum/testing/model/scenario scenario.xsd">

    <overview>
        <name>Assert NotEqual - Variations</name>
        <description>
            This scenario verifies notEqual assertion using CSV-driven variations.

            Test flow:
            1. Load variations from CSV file.
            2. Extract UI value using locator from CSV.
            3. Execute notEqual assertion for each variation.

            This scenario validates:
            - CSV-based variable injection
            - Dynamic test execution per variation
            - Correct notEqual behavior for multiple data sets
        </description>
    </overview>

    <settings>
        <variations>not_equal_variations.csv</variations>
        <tags>web</tags>
    </settings>

    <include comment="Include login steps" scenario="/ui/includes/login_web/scenario.xml"/>

    <web comment="Start WEB script">

        <var comment="{{varComment}}" name="uiValue">
            <element>
                <attribute name="{{attributeName}}"
                           locator="{{locator}}"
                           locatorStrategy="xpath"/>
            </element>
        </var>

        <assert comment="{{requestComment}}">
            <notEqual comment="{{notEqualComment}}">
                <content>{{uiValue}}</content>
                <content>{{value2}}</content>
            </notEqual>
        </assert>

    </web>

</scenario>