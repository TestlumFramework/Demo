<scenario xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xmlns="http://www.knubisoft.com/testlum/testing/model/scenario"
          xsi:schemaLocation="http://www.knubisoft.com/testlum/testing/model/scenario scenario.xsd">

    <overview>
        <name>Assert Title - Condition</name>
        <description>
            This scenario verifies conditional execution of title assertion.

            Test flow:
            1. Execute title assertion with condition=true.
            2. Execute invalid title assertion with condition=false.

            This scenario validates:
            - Condition handling
            - Skipping logic
        </description>
    </overview>

    <settings>
        <tags>web</tags>
    </settings>

    <include comment="Include login steps" scenario="/ui/includes/login_web/scenario.xml"/>

    <web comment="Start WEB script">

        <condition comment="Create true condition (should execute)"
                   name="trueCondition"
                   spel="true"/>

        <condition comment="Create false condition (should skip)"
                   name="falseCondition"
                   spel="false"/>

        <assert comment="Assert should execute because condition=true"
                condition="trueCondition">
            <title comment="Check that page title equals expected value">
                <content>V-Dashboard</content>
            </title>
        </assert>

        <assert comment="Assert should NOT execute because condition=false"
                condition="falseCondition">
            <title comment="Invalid title but should be skipped">
                <content>INVALID</content>
            </title>
        </assert>

    </web>

</scenario>