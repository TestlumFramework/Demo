<scenario xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xmlns="http://www.knubisoft.com/testlum/testing/model/scenario"
          xsi:schemaLocation="http://www.knubisoft.com/testlum/testing/model/scenario scenario.xsd">

    <overview>
        <name>Clear - Variations</name>
        <description>
            This scenario verifies clear command using CSV-driven variations.

            Test flow:
            1. Load data from CSV file.
            2. Navigate to page using injected path.
            3. Input value using injected data.
            4. Execute clear command using injected locator and highlight.
            5. Verify clear execution using assertion.

            This scenario validates:
            - CSV-based variable injection
            - Dynamic locator and value usage
            - Clear command execution with different data sets
        </description>
    </overview>

    <settings>
        <variations>clear_variations.csv</variations>
        <tags>web</tags>
    </settings>

    <include comment="Include login steps"
             scenario="/ui/includes/login_web/scenario.xml"/>

    <web comment="Start WEB script">

        <navigate comment="{{navigateComment}}"
                  command="to"
                  path="{{path}}"/>

        <input comment="{{inputComment}}"
               locator="{{locator}}"
               value="{{value}}"/>

        <clear comment="{{clearComment}}"
               locator="{{locator}}"
               highlight="true"/>

        <assert comment="{{assertComment}}">
            <notEqual comment="{{notEqualComment}}">
                <content>{{value}}</content>
                <content>TestUsers</content>
            </notEqual>
        </assert>

    </web>

</scenario>