<scenario xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xmlns="http://www.knubisoft.com/testlum/testing/model/scenario"
          xsi:schemaLocation="http://www.knubisoft.com/testlum/testing/model/scenario scenario.xsd">

    <overview>
        <name>HotKey - Repeat Simple</name>
        <description>
            This scenario verifies repeat execution using space key.

            Test flow:
            1. Navigate to UI Elements page.
            2. Focus input field.
            3. Repeat space key.
            4. Verify accumulated result.

            This scenario validates:
            - repeat execution
            - hotKey stability
        </description>
    </overview>

    <settings>
        <tags>web</tags>
    </settings>

    <include comment="Include login step"
             scenario="/ui/includes/login_web/scenario.xml"/>

    <web comment="Start WEB script">

        <navigate comment="Navigate to UI Elements page"
                  command="to"
                  path="/ui-elements"/>

        <wait comment="Wait for page load" time="1"/>

        <input comment="Set initial value"
               locator="uiElements.hotKeyInput2"
               value="A"/>

        <click comment="Focus input"
               locator="uiElements.hotKeyInput2"/>

        <repeat comment="Repeat space key 3 times"
                times="3">

            <hotKey comment="Press space">
                <space comment="Add space to current value"/>
            </hotKey>

        </repeat>

        <assert comment="Verify spaces added">
            <attribute comment="Check final value"
                       name="value"
                       locator="uiElements.hotKeyInput2">
                <content>A   </content>
            </attribute>
        </assert>

    </web>

</scenario>