<scenario xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xmlns="http://www.knubisoft.com/testlum/testing/model/scenario"
          xsi:schemaLocation="http://www.knubisoft.com/testlum/testing/model/scenario scenario.xsd">

    <overview>
        <name>GraphQL Repeat Times</name>
        <description>
            This scenario verifies GraphQL execution inside repeat block using times.

            Test flow includes:
            1. Execute GraphQL mutation inside repeat block 3 times.
            2. Create the same book on each iteration.
            3. Verify response status = 200 for each iteration.
            4. Retrieve all books after repeat execution.
            5. Verify that 3 books were created in memory.
            6. Reset GraphQL storage after test.

            This scenario validates:
            - GraphQL mutation execution inside repeat block
            - Stable repeated execution using times
            - Data accumulation across iterations
            - Correct final storage state
        </description>
        <developer>Vadym Kostenko</developer>
    </overview>

    <settings active="false">
        <tags>graphQL</tags>
    </settings>

    <repeat comment="Step 1-3. Execute GraphQL create mutation multiple times"
            times="3">

        <graphql comment="Create book inside repeat-times block"
                 alias="MEGA_GQL_APP">
            <post endpoint="/graphql">
                <response code="200" mode="strict"/>
                <body>
                    <from file="request_1.json"/>
                </body>
            </post>
        </graphql>

    </repeat>

    <graphql comment="Step 4-5. Verify all repeated books were created"
             alias="MEGA_GQL_APP">
        <get endpoint="/graphql">
            <response code="200" file="expected_4.json"/>
            <param name="query" data="{ getAllSelfBooks { title author number } }"/>
        </get>
    </graphql>

    <graphql comment="Step 6. Reset GraphQL storage after repeat-times test"
             alias="MEGA_GQL_APP">
        <post endpoint="/graphql">
            <response code="200" file="expected_5.json"/>
            <body>
                <raw>
                    {
                    "query": "mutation { resetSelfBooks }"
                    }
                </raw>
            </body>
        </post>
    </graphql>

</scenario>