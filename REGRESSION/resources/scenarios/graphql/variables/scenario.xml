<scenario xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xmlns="http://www.knubisoft.com/testlum/testing/model/scenario"
          xsi:schemaLocation="http://www.knubisoft.com/testlum/testing/model/scenario scenario.xsd">

    <overview>
        <name>GraphQL Variables</name>
        <description>
            This scenario verifies GraphQL request execution using variables.

            Test flow includes:
            1. Create variables for comments, endpoint, book fields and expected files.
            2. Execute GraphQL mutation using variable values.
            3. Verify create response = 200 using variable-based expected file.
            4. Reset GraphQL storage using variable-based endpoint.

            This scenario validates:
            - Variable injection into comment
            - Variable injection into GraphQL endpoint
            - Variable injection into GraphQL request body
            - Variable injection into expected file names
            - Variable reuse across GraphQL mutation flow
        </description>
        <developer>Vadym Kostenko</developer>
    </overview>

    <settings>
        <tags>graphql</tags>
    </settings>

    <var comment="Endpoint variable"
         name="graphqlEndpoint">
        <constant value="graphql"/>
    </var>

    <var comment="Create mutation comment"
         name="createComment">
        <constant value="Create GraphQL book using variables"/>
    </var>

    <var comment="Reset comment"
         name="resetComment">
        <constant value="Reset GraphQL storage"/>
    </var>

    <var comment="Book title"
         name="bookTitle">
        <constant value="Variable GraphQL Book"/>
    </var>

    <var comment="Book author"
         name="bookAuthor">
        <constant value="Vadym Kostenko"/>
    </var>

    <var comment="Book number"
         name="bookNumber">
        <constant value="77"/>
    </var>

    <graphql comment="{{createComment}}"
             alias="MEGA_GQL_APP">
        <post endpoint="/{{graphqlEndpoint}}">
            <response code="200" file="expected_7.json"/>
            <body>
                <raw>
                    {
                    "query": "mutation { addSelfBook(newBook: { title: \"{{bookTitle}}\", author: \"{{bookAuthor}}\", number: {{bookNumber}} }) { title author number } }"
                    }
                </raw>
            </body>
        </post>
    </graphql>

    <graphql comment="{{resetComment}}"
             alias="MEGA_GQL_APP">
        <post endpoint="/{{graphqlEndpoint}}">
            <response code="200" file="expected_8.json"/>
            <body>
                <raw>
                    {
                    "query": "mutation { resetSelfBooks }"
                    }
                </raw>
            </body>
        </post>
    </graphql>

</scenario>