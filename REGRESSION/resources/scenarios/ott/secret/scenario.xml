<scenario xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xmlns="http://www.testlum.com/testing/model/scenario"
          xsi:schemaLocation="http://www.testlum.com/testing/model/scenario scenario.xsd">

    <overview>
        <description>Verify Ott generates code directly from inline secret, bypassing alias resolution</description>
        <name>Ott Test with inline secret</name>
    </overview>

    <settings>
        <tags>ott</tags>
    </settings>

    <ott comment="Generate OTP code directly from inline secret" name="secretCode"
         secret="JBSWY3DPEHPK3PXP"/>

    <ott comment="Generate OTP code via alias with the SAME underlying secret, for cross-check" name="aliasCode"
         alias="OTT_QA"/>

    <condition comment="Verify secretCode has correct format" name="secretFormatCondition"
               spel="'{{secretCode}}'.matches('^[0-9]{6}$')"/>

    <assert comment="Check that secretFormatCondition is true">
        <equal comment="secretFormatCondition equals true">
            <content>{{secretFormatCondition}}</content>
            <content>true</content>
        </equal>
    </assert>

    <assert comment="Same secret via inline attribute and via alias must yield the same code (same key material)">
        <equal comment="secretCode must equal aliasCode">
            <content>{{secretCode}}</content>
            <content>{{aliasCode}}</content>
        </equal>
    </assert>

    <ott comment="Generate OTP code from a DIFFERENT inline secret" name="secretCodeOther"
         secret="GEZDGNBVGY3TQOJQGEZDGNBVGY3TQOJQ"/>

    <condition comment="Codes for different secrets must differ" name="codesDiffer"
               spel="!('{{secretCode}}'.equals('{{secretCodeOther}}'))"/>

    <assert comment="Check codesDiffer is true">
        <equal comment="codesDiffer equals true">
            <content>{{codesDiffer}}</content>
            <content>true</content>
        </equal>
    </assert>

</scenario>