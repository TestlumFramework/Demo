<scenario xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xmlns="http://www.knubisoft.com/testlum/testing/model/scenario"
          xsi:schemaLocation="http://www.knubisoft.com/testlum/testing/model/scenario scenario.xsd">

    <overview>
        <name>Auth Custom AutoLogout Disabled</name>
        <description>
            This scenario verifies Custom Authentication behavior when autoLogout is disabled.

            Test flow includes:
            1. Send request without Authorization header → expect 401.
            2. Apply Custom Auth via auth tag.
            3. Send GET request inside auth block → expect 200.
            4. Verify protected response body and X-Auth-Type header.
            5. Exit auth block.
            6. Send request again outside auth block → expect 200 because autoLogout=false.
            7. Reset test data.

            This scenario validates:
            - Custom auth login flow via auth command
            - Custom Authorization header injection inside auth block
            - Protected endpoint access with valid custom token
            - Authorization persistence after leaving auth block
            - Continued authorized access when autoLogout is disabled
        </description>
        <developer>Vadym Kostenko</developer>
    </overview>

    <settings>
        <tags>auth</tags>
    </settings>

    <http comment="Unauthorized request should return 401 before auth"
          alias="MEGA_APP_CUSTOM_NO_LOGOUT">
        <get endpoint="/api/test/auth/custom/resource">
            <response code="401" file="expected_1.json"/>
        </get>
    </http>

    <auth comment="Apply Custom Auth header"
          apiAlias="MEGA_APP_CUSTOM_NO_LOGOUT"
          credentials="http-auth-user-custom.json"
          loginEndpoint="/api/test/auth/custom/login">

        <http comment="Access protected custom resource inside auth block"
              alias="MEGA_APP_CUSTOM_NO_LOGOUT">
            <get endpoint="/api/test/auth/custom/resource">
                <response code="200" file="expected_3.json"/>
            </get>
        </http>

    </auth>

    <http comment="Authorized request should still work after auth block because autoLogout is disabled"
          alias="MEGA_APP_CUSTOM_NO_LOGOUT">
        <get endpoint="/api/test/auth/custom/resource">
            <response code="200" file="expected_4.json"/>
        </get>
    </http>

    <http comment="Reset custom auth state after test"
          alias="MEGA_APP_CUSTOM_NO_LOGOUT">
        <delete endpoint="/api/test/auth/custom/reset">
            <response code="200"/>
        </delete>
    </http>

</scenario>